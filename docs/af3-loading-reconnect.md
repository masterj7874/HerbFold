# AF3 실행·결과 재연결과 진행 표시 — 2026-09-14

Bruceajavanone A(대규모 탐색 ID 87, C38H56O8)와 사람 EGFR `P00533`의 작업 `31dfd5615cb848c5a98c78dc657c3692`를 확인했습니다. CPU MSA·템플릿 검색은 897.93초, AF3 추론 단계는 177.95초에 완료됐고, 작업은 12:05:27 KST에 정상 종료했습니다. 대표 구조와 5개 표본이 생성됐으며 선택 성분·표적 일치 검사를 통과했습니다. 이번 작업에는 GPU 메모리 부족이나 Traceback 오류가 없었습니다.

별도로 화면 재연결 결함을 실제 브라우저에서 재현했습니다. 저장된 선택을 복원하며 발생한 최초 목록·구조 요청은 인증 토큰 입력 전이라 401 응답을 받았습니다. 이후 엔진 설정에서 연결 상태를 갱신해도 계산 패널과 구조 조회에는 그 갱신이 전달되지 않았습니다. 헤더는 복구됐지만 계산 목록은 이전 실패 상태에 머물렀고, 수동 작업 새로고침을 눌러야 완료 상태와 실제 구조가 표시됐습니다.

연결 갱신을 계산 목록·구조·진단 조회에 연결하고, 구조 응답 대기에도 30초 시간 제한을 적용했습니다. 탭 복귀나 연결 회복으로 다시 조회하더라도 계산을 중복 제출하지 않습니다. 계산 중이라는 상태와 화면이 서버 응답을 기다리는 상태를 구분합니다.

분자 특성 패널은 조회에 성공해도 이전 오류 문구를 지우지 않던 별도 표시 문제가 있었습니다. 재조회와 성공 시 오류를 초기화하고, 재연결 후 실제 분자식 표시와 인증 오류 제거도 브라우저 검증에 포함했습니다.

진행 정보에서는 화면이 데이터를 받은 시각과 실행 프로세스의 마지막 확인 시각을 따로 표시합니다. 오래된 작업 정보를 다시 내려받았다는 이유만으로 프로세스가 계속 진행 중이라고 표시하지 않습니다. 전체·단계별 경과와 실제 완료 시간, 관측한 세부 단계를 보여주며 임의의 진행률이나 완료 예정 시각을 만들지 않습니다.

AF3의 표준 출력은 파일로 연결될 때 버퍼링되어 특징 생성·모델 추론·출력 저장 메시지가 늦게 표시될 수 있었습니다. 이후 실행에서는 진행 출력이 즉시 로그에 전달되도록 설정하고, 실제 로그에서 확인한 세부 단계와 검색 완료 기록을 전달합니다. 수치 계산 설정과 정상 계산의 중단 조건은 바꾸지 않습니다.

기존 계산 입력·검색 캐시·모델·로그는 보존했습니다. 이번 소프트웨어 검증을 위해 새 과학 계산을 제출하지 않았으며, 실제로 완료된 위 작업을 조회해 화면 동작을 확인했습니다.

관련 Python 테스트 134개와 프런트엔드 테스트 57개, Ruff와 프로덕션 빌드가 통과했습니다. 서버를 `0.0.0.0:9018`에 적용한 뒤 기존 작업 상태 보존, AF3 실행 준비 상태, 완료 구조의 성분·표적 일치를 다시 확인했습니다. [서버 검증](af3-reconnect-server-verification.json).

실제 브라우저에서 토큰 재연결 후 목록·구조·진단의 자동 복구, 연결 회복·탭 복귀 이벤트의 중복 조회 통합, 로그 응답 정지 중에도 완료 구조 버튼 표시, 구조 응답 정지 후 시간 제한과 재연결 복구를 확인했습니다. 데스크톱과 모바일에서 결과 표시 및 가로 넘침 없음을 검사했고 JavaScript 오류는 없었습니다. 응답 정지는 브라우저에서만 재현했으며 실제 계산 데이터는 변경하지 않았습니다. [수정 전 재현](af3-loading-reconnect-before.json), [브라우저 검증과 화면](af3-reconnect-verification.json), [실제 로그 단계 파싱 검증](af3-egfr-progress-replay.json).
